<Window x:Class="accounting_platform.view.Test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Test" Height="450" Width="800"
        x:Name="root"
        >
    <Grid>
       <!-- <TextBlock 
       Text="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, Path=title}"
      /> -->

        <TextBlock 
            Text="{Binding ElementName=root, Path=title }"
        />
        <Button x:Name="buttonNewTitle" Click="buttonNewTitle_Click" Width="100" Height="100">Новый заголовок</Button>
   </Grid>
</Window>
